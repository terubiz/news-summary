export default function LoginPage() {
  return (
    <main className="flex items-center justify-center min-h-screen bg-gray-50">
      <div className="w-full max-w-md p-8 bg-white rounded-lg shadow-sm border border-gray-200">
        <h1 className="text-2xl font-semibold text-gray-900 mb-6">ログイン</h1>
        <p className="text-gray-500 text-sm">認証画面は後続タスクで実装されます。</p>
      </div>
    </main>
  )
}
